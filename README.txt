script.py is the main function which calls all the other functions
In script.py change the file names to be read accordingly
If you want to create a new mask comment the 8th line in the script and uncomment the 7th line

Also the offset in X and Y are set here. For the minion blending the offsets are X = 300 and Y = 240 

For other image the offsets are X = 150 and Y = 300

The clipping method used in seamlessCloningPoisson to constrain the pixel values.

 